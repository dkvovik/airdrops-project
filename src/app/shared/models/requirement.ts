export class Requirement {
  name?: string;
  follow?: boolean;
  retweet?: boolean;
  tweet?: boolean;
  joinGroup?: boolean;
  joinChannel?: boolean;
  singleShare?: boolean;
  posting?: boolean;
}
